"""Mainpage configure for 'mainpage' app."""

from django.apps import AppConfig


class MainpageConfig(AppConfig):
    """Class for configuring 'mainpage' app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "mainpage"
